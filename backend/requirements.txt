# The Inclusive Citizen — Backend Dependencies
# PRD Appendix C

# Web framework
fastapi>=0.135.0,<1.0
uvicorn[standard]>=0.34.0,<1.0
python-multipart>=0.0.22

# Data validation
pydantic>=2.10.5,<3.0

# Google Cloud Platform
google-cloud-aiplatform>=1.60.0
google-cloud-translate>=3.15.0
google-cloud-speech>=2.26.0
google-cloud-texttospeech>=2.16.0
google-genai>=1.0.0
google-cloud-discoveryengine>=0.11.0  # Vertex AI Search (Discovery Engine) — retrieval and ingestion
google-cloud-storage>=2.16.0          # GCS upload for PDF ingestion pipeline

# LLM / Transformers (NLLB-200 fallback)
transformers>=4.40.0
sentencepiece>=0.2.0
torch>=2.2.0

# PDF parsing
# No longer used in the RAG ingestion pipeline (Vertex AI Search handles chunking internally).
# Retained for scripts/bulk_ingest.py and any future direct PDF parsing needs.
pymupdf>=1.24.0

# Dialect detection
lingua-language-detector>=2.0.0
langdetect>=1.0.9

# NLP
spacy>=3.7.0
sentence-transformers>=3.0.0
textstat>=0.7.3

# Database — retained for document_metadata table (doc_type lookup) and conversation context
supabase>=2.5.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0
hijri-converter>=2.3.0
