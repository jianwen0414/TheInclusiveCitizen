# Demo audio (optional)

`mak_cik_rohani.mp3` can live here for:

- **Playwright E2E** (`npm run demo:voice-e2e`) — the script also accepts `ElevenLabs_*Mak Cik Rohani*.mp3` in the repo root.

Copy from the ElevenLabs export (repo root):

```powershell
.\scripts\copy_rohani_demo_audio.ps1
```

See **`docs/DEMO_MAK_CIK_ROHANI.md`** for the full voice demo flow.


Steps to run:

1. npm run demo:chrome-cdp 
2. npm run demo:voice-e2e:attach